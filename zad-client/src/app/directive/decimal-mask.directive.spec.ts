import { DecimalMaskDirective } from './decimal-mask.directive';

describe('DecimalMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new DecimalMaskDirective();
    expect(directive).toBeTruthy();
  });
});
